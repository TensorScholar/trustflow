# TrustFlow release-only toolchain constraints.
# Keep ordinary package builds flexible; the release workflow applies this lock to
# its frontend environment and isolated PEP 517 build dependencies.
build==1.6.0
packaging==26.3
pip==26.2.1
pyproject-hooks==1.2.0
setuptools==84.0.0
twine==6.2.0
wheel==0.48.0
